import NFTCardsList from "@/components/NFTCardsList"; // Assuming the correct import

const Marketplace = () => {
  return (
    <div>
      <div className="container mx-auto px-4">
        <NFTCardsList />
      </div>
    </div>
  );
};

export default Marketplace;
